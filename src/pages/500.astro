---
import RootLayout from "~/layouts/RootLayout.astro";
import RemixIcon from "~/components/RemixIcon.astro";

const isHome = Astro.url.pathname === '/';
---

<RootLayout
  title="Error"
  class="max-w-md m-auto flex flex-col gap-8 pt-20 pb-60 px-3"
>
  <h1 class="text-primary-purple font-serif font-bold text-4xl">
    Whoops, we can't load this page right now.
  </h1>
  {isHome ? (
    <p>While we work on getting this fixed check out our shows and classes directly on TicketLeap, our event ticketing provider.</p>
    <div role="group" class="flex gap-6 flex-col lg:flex-row">
      <a class="coral_btn_link min-w-0 w-full lg:w-full" href="https://events.ticketleap.com/events/catch">
        Shows
      </CoralButtonLink>
      <a class="coral_btn_link min-w-0 w-full lg:w-full" href="https://events.ticketleap.com/events/catch-u">
        Classes
      </CoralButtonLink>
    </div>
  ) : (
    <p>Let's get you back on track.</p>
    <a class="coral_btn_link w-fit min-w-none" href="/">
      <RemixIcon icon="home-4" variant="line" /> Go Home
    </CoralButtonLink>
  )}
</RootLayout>
